# Session Compaction — MemodoAI "What's New (July 2026)" tutorial (guide + video + folder reorg) — 2026-07-22_10-19-36

## Session Context

- Context trigger: high utilization (adhoc-compact invoked)
- Working on: building a standalone **"What's New (July 2026)"** MemodoAI update deliverable (written
  guide + companion video assets), then reorganizing the whole `docs/memodo-ai-tutorial/` folder into
  per-recording folders with shared root tooling.
- Current phase: **ad-hoc / follow-up docs work — NOT an SDD phase.** (SDD/ artifacts present are from
  unrelated past features 008–018.)
- Branch: `pablo-oliva/docs-memodo-ai-tutorial`. All changes UNCOMMITTED (user commits/pushes manually).

## Work Summary

### Completed This Session
- **Scoped + planned** the deliverable → `docs/memodo-ai-tutorial/updates-2026-07/UPDATE-PLAN-v0.8.7-m365.md` (source of truth).
- **Wrote the guide** `updates-2026-07/updates-july-2026.md` — 7 sections: (1) what changed (2) Microsoft 365 [deep] (3) Skills [deep + create-a-skill walkthrough] (4) chaining & memory (5) Projects (6) quality-of-life (7) wrap-up. UI verified live on chat-test.
- **Captured 14 screenshots** in `updates-2026-07/screenshots/` (`01`–`12`, `14` + extras `03b`, `07b`). Dropped `13` code-artifacts.
- **Corrected several claims via live verification** (see Key Discoveries).
- **Drafted video assets:** `updates-2026-07/recording-script.md` (7-chapter shot list) + `recording-narration.md` (~8–9 min).
- **Folder reorg (2 rounds):** round 1 made `updates-2026-07/` self-contained; round 2 (Option A, this session's last major work) reorganized `memodo-ai-tutorial/`:
  - Original guide → `getting-started/` (via `git mv`, history preserved).
  - `updates-2026-07/` keeps its content; `UPDATE-PLAN` moved INTO it; its tooling copy removed.
  - **Shared tooling at root**, parameterized by recording folder: `node tooling/tts/narrate.mjs <recording>`, `bash tooling/transcribe.sh <recording>` (both verified via `--dry-run`).
  - New root `README.md` (recordings index + conventions); `obs-recording.md` + `video-pipeline.md` stay at root (shared).
  - `.gitignore`: one glob `docs/memodo-ai-tutorial/*/audio|video/*` for all recordings.
  - All internal references fixed; two broken-ref sweeps clean.
- Updated memory `project_july2026_updates_tutorial` throughout.

### In Progress
- None active — reorg complete and verified (git shows renames; scripts resolve both recordings; gitignore correct).

### Remaining (the video + publish pipeline)
1. **Record the documentary track in OBS** (operator-driven; drive browser through `recording-script.md` 7 chapters). A ready-to-paste continuation prompt was produced in-session.
2. Render narration (`narrate.mjs updates-2026-07`) → assemble cut → transcript EN/DE + `chapters.json` → transcode to web MP4.
3. Bilingual HTML export of the guide.
4. `/guide` **selector** frontend change (`Guide.tsx`: "Getting Started" | "What's New (July 2026)").
5. Deploy — chat-test now; **prod publish coupled to the pending v0.8.7+M365 prod cutover**.
6. Small **evergreen-guide corrections** (getting-started/): v0.8.5→v0.8.7 footer, instant titles, composer buttons (Search/File Search/MCP Servers).
7. Reconcile stale "verification pending" header in `docs/m365-mcp-features.md`.
- Test artifacts left on chat-test per user's OK: 2 memory entries (Frontend team / bulleted answers), "Q3 Planning" project, ~5 test chats, `~/Downloads/MemodoAI-sample-roadmap.xlsx`.

## Key Discoveries
- **code-as-artifacts NOT available** in our deployment (no model/endpoint carries the Artifacts capability) → dropped from scope (`13` screenshot + §6 bullet removed).
- **`memory` IS a real agent capability in our build** (`packages/data-provider/src/config.ts` `AgentCapabilities.memory` + `packages/api/src/agents/memory.ts` `buildInlineMemoryTool`) — public docs (13 caps) lag. Surfaces via the assistant's "Saved: …" reply + Memories panel; **no per-agent toggle**.
- **M365 = MCP server**, enabled via composer **MCP Servers** dropdown → tick Microsoft365; lazy-connects (gray dot + plug → **Initialize** → green Active); tool use shows "**Ran <tool> in Microsoft365**". Read-only; runs as the signed-in user.
- **Chaining works** (confirmed: `Chain Test Primary` + `Uppercase Bot`); Agent Builder → Advanced → Multi-agent orchestration → Chain.
- Skills = **left-rail panel + `$` picker** (composer Skills button is conditional); create form is Name/Description/Instructions (no 3-mode invocation selector); body convention "When to use / Procedure / Output format".
- Browser gotchas: composer first-keystroke sometimes doesn't register; `file_upload` MCP tool works against the hidden input with a scratchpad-staged file (no OS dialog).

## Critical Review Status
- Review performed: N/A (docs work; no `SDD/reviews/CRITICAL-*.md` for this task).
- Unresolved findings: none.

## Critical References
- `docs/memodo-ai-tutorial/README.md` — new root index (recordings + shared conventions)
- `docs/memodo-ai-tutorial/updates-2026-07/UPDATE-PLAN-v0.8.7-m365.md` — plan/status (source of truth)
- `docs/memodo-ai-tutorial/updates-2026-07/{updates-july-2026.md, recording-script.md, recording-narration.md, screenshots/}`
- `docs/memodo-ai-tutorial/tooling/tts/narrate.mjs` + `tooling/transcribe.sh` — shared, parameterized by `<recording>`
- `docs/memodo-ai-tutorial/getting-started/` — original evergreen guide (moved)
- memory: `project_july2026_updates_tutorial`

## Next Session

**Resume From:**
- Video documentary-track recording in OBS, using the in-session continuation prompt (references `updates-2026-07/recording-script.md`, `recording-narration.md`, and `docs/memodo-ai-tutorial/updates-2026-07/UPDATE-PLAN-v0.8.7-m365.md`). Environment: chat-test.memodo.de, signed in via Microsoft, window 1440×900.

**Immediate Priorities:**
1. Record the 7 chapters in OBS (operator owns record button; drive browser per shot list).
2. Then: narration render → assemble → transcript/chapters → transcode → HTML export → /guide selector → deploy.

**Open Questions:** none blocking. All scope decisions settled (standalone deliverable; title "What's New (July 2026)"; /guide selector = Option A; M365 via MCP Servers menu; code-artifacts out; chaining in; shared root tooling). Do not re-open.

## Notes
- All work UNCOMMITTED on `pablo-oliva/docs-memodo-ai-tutorial`; user commits/pushes manually.
- `git mv` preserved history for the getting-started/ move (renames in `git status`).
