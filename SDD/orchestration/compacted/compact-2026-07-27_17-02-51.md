# Session Compaction — v0.8.7 + M365 PROD CUTOVER (prep complete, window open) — 2026-07-27 17:02

## Session Context

- Working on: **production cutover of `feature/015-m365-obo-v0.8.7`** to `chat.memodo.de`. Ad-hoc deploy
  work, NOT an SDD phase (SDD-FLOW 019 closed 2026-07-23, `progress.md:114`).
- Current phase: **deploy prep COMPLETE → execution PENDING.** The 17:00 window is open as of this file.
- Prod has **NOT** been modified this session. All work was read-only recon + local commits.

## Work Summary

### Completed This Session

1. **Wrote the three-week retro** (separate deliverable, DONE): Obsidian vault
   `Memodo/MemodoAI - v0.8.7 Upgrade and M365 Integration Retro.md`, linked from `Logger/log.md`
   (entry `2026-07-27 11:55`) and added to `MemodoAI.md` frontmatter `sibling:` list.
2. **Deploy checklist item 4 RESOLVED** (was the last ⚠️ OPEN item) — decision + evidence below.
3. **Two commits on `pablo`** (local only, NOT pushed — `pablo` is ahead 2 of `origin/pablo`):
   - `9cf4d4c12` `fix(deploy)`: `prod-sync.sh` PROD_HOST default `Hetzner-personal` → `memodo-eng-prod`
     (the old default is not in ssh config — bare `./prod-sync.sh` fails to resolve).
   - `516972353` `docs(deploy)`: item 4 closed, items 1/1c marked resolved-no-action, §5 rewritten as
     the cutover runbook, §7 rollback made concrete.
4. **Reverted** an exploratory `docker-compose.prod.yml` edit → branch keeps build-from-source.

### In Progress / Remaining

**Nothing done on prod. The entire runbook (§5 of the deploy checklist) is still to execute.**

## Key Discoveries (all empirically verified this session — do not re-derive)

### 1. Item 4 decided: BUILD FROM SOURCE on prod. Registry image REJECTED on evidence.
- Branch merged upstream `main` at **`8fcb77fe6`** (2026-07-05) = **v0.8.7 GA + ~96 post-GA commits**,
  so the registry `v0.8.7` tag (GA, 2026-06-24) is **OLDER than our tree**.
- No published image matches: checked every `v0.8.x` tag on `danny-avila/librechat` **and** all 101 SHA
  tags on `danny-avila/librechat-dev` (incl. `8fcb77fe6` and its 30 predecessors) — no match.
- Booting the GA image with our dist bind-mounted → **`MODULE_NOT_FOUND:
  @langchain/langgraph-checkpoint-mongodb`**. It is an **eager top-level require** in
  `packages/api/dist/index.cjs` line ~117 (beside `url`/`dedent`) → **crash-loop at boot**, not lazy.
  Module arrives with upstream's post-GA HITL checkpointer (`6dbf9d5ad`/`ed8547018`/`84fa6aa82` →
  `packages/api/src/agents/checkpointer.ts`), which GA predates.
- Silent skew behind it: GA image ships `@librechat/agents` **3.2.46** vs our declared **^3.2.57**.
- Overlaying both gaps (~40 MB) DOES resolve ("RESOLVE OK with both overlays") but was rejected: puts
  prod on an untested base image + hand-maintained `node_modules`.
- **The OOM argument against building on prod was WRONG.** Server is *named* `ubuntu-4gb-p-ai-chat` but
  resized: **15 GB RAM / 8 GB free, 21 GB free disk**. Building here is fine.

### 2. ⚠️ STALE-DIST TRAP IS LIVE — the dist rsync is NOT optional
Prod's bind-mounted overlays are **v0.8.5-era**: `packages/api/dist/index.js` (2026-05-19), **no
`index.cjs`**, Finding #6 symbol count **0**. They mount *over* the freshly built image, so building
alone leaves the container loading a stale `index.js` → crash-loop. **Build AND rsync, in that order.**
Same trap in reverse on rollback → back the dist up before overwriting.

### 3. `.env.prod` needs ONE edit, not three (verified on the box)
- **Item 1 `OPENID_SCOPE`: ✅ ALREADY CORRECT** — contains `api://…/access_as_user`. The long-standing
  "clone/prod discrepancy" risk was a stale *clone*, not a broken prod. **No edit.**
- **Item 1c OBO userinfo: ✅ ALREADY ALIGNED** (`FOR_USERINFO_REQUIRED=true`, scope `User.Read`,
  `OPENID_REUSE_TOKENS=true`, `USE_ENTRA_ID_FOR_PEOPLE_SEARCH=true`). **No edit.**
- **Item 1b: ❌ BOTH legacy Azure vars SET and non-empty → THE ONLY EDIT NEEDED.**
- **Item 1d** (`memory.agent.enabled`) absent from prod yaml → **travels via `git pull`** ✅.
- `.env.prod` is currently `uid=1000 gid=1000` (`librechat`), mode 600. Re-chown ONLY after editing.

### 4. Branch flow is trivial
`memodo` is a **strict ancestor** of `pablo` (0 behind / 692 ahead, merge-base == `memodo` tip
`be8c73562`) → **clean fast-forward, zero conflicts**. Prod tracks `memodo`, clean tree → also FFs.

### 5. Verified prod state (2026-07-27, pre-window)
Branch `memodo` @ `be8c73562` · api on `registry.librechat.ai/danny-avila/librechat:v0.8.5`
(restarts 0, up since 07-04) · compose **v5.3.0** · 15 GB RAM / 21 GB free disk ·
**v0.8.5 rollback image present locally (3.48 GB) — DO NOT `docker image prune`** ·
`guide-media/` present (64 MB → will refresh to 91 MB) · nightly Mongo backup cron 02:00 ·
Mongo container = `chat-mongodb` · `librechat:upgrade` NOT yet built on prod.

### 6. Local build state is GOOD — no rebuild needed
All four dist dirs present and fresh; `grep -c 'Skipping background reconnect for OBO server'
packages/api/dist/index.cjs` = **1**; `isOboTokenNearExpiry` ×2; `client/dist` (19:33) postdates the
last client commit (19:32) and carries `updates-2026-07`/`guide-media`/`com_ui_guide_july2026_title`.
⚠️ If a rebuild DOES become necessary: **`unrun` is not installed** → `npm install --no-save
'unrun@^0.3.0'` then `git checkout -- package-lock.json`. Node 22.18 must be active.

### 7. SSH is IP-allowlisted and your IP rotates
Hetzner firewall `librechat` (id 10506648) allowlists :22 per-IP. IP rotated
`79.205.210.123` → `93.235.188.52` today; user re-added it via the Hetzner console. **If ssh hangs,
it's the firewall, not the server** — HTTPS/443 stays open regardless, so `curl https://chat.memodo.de`
returning 200 does NOT mean ssh works. Firewall covers 4 servers (prod + test + 2).

## Critical Review Status

- Review performed this session: **no** (deploy work, not an SDD phase).
- Existing `SDD/reviews/CRITICAL-*.md` are historical (latest relevant:
  `CRITICAL-RESEARCH-memory-write-fix-20260722.md`, SDD-019, closed).
- Unresolved findings: none blocking. Non-blocking carry-overs: Finding #4 (RAG `DELETE /documents`
  404, Low, monitor) and the inline memory-as-capability path (toggle ON) never verified.

## Critical References

- **THE RUNBOOK:** `docs/deploy-checklist-015-m365-obo-v0.8.7.md` §5 (six steps) — authoritative.
  §4 = image decision + evidence; §6 = post-deploy verification; §7 = concrete rollback.
- `docker-compose.prod.yml:16-25` — api `build:` + `image: librechat:upgrade` (build-from-source, keep).
- `docker-compose.prod.yml:84-113` — `volumes: !override`, the six bind mounts incl. the three dist
  overlays into `/app/node_modules/...` (they are **symlinks** to `/app/packages/...`; Docker resolves
  through them — proven working).
- `prod-sync.sh:33` PROD_HOST default; `PATHS=()` = the 6 synced paths.
- `librechat.yaml:226` capabilities (8, incl. `tools`), `:271` `memory.agent.enabled: true`, `:279`
  `modelSpecs.enforce: false`.
- `CLAUDE.md` "Production Deploy Discipline"; `docs/fork-upstream-divergences.md` (7 fork hooks).

## Next Session

**Resume From:** step 0 of the runbook. Nothing has been executed.

**Immediate Priorities:**
1. `ssh memodo-eng-prod hostname` — confirm the firewall still lets us in (IP may have rotated again).
2. **Local:** `git checkout memodo && git merge --ff-only pablo && git push origin memodo`
   (this also carries commits `9cf4d4c12` + `516972353`; `pablo` itself was never pushed).
3. **Backups:** Mongo dump (`./scripts/backup-mongodb.sh`);
   `tar czf ~/dist-v085-$(date +%F).tgz packages/api/dist packages/data-schemas/dist packages/data-provider/dist client/dist`;
   `cp .env.prod ~/.env.prod.bak-$(date +%F)`.
4. **Prod:** `git pull` (FF) → `./prod.sh build api` (non-disruptive, v0.8.5 keeps serving).
5. **Local:** `PROD_HOST=memodo-eng-prod ./prod-sync.sh --no-restart` → verify on prod that
   `packages/api/dist/index.cjs` now EXISTS and the Finding #6 grep = 1.
6. **Prod:** empty `AZURE_OPENAI_ENDPOINT=` + `AZURE_OPENAI_API_KEY=` in `.env.prod` →
   `chown 1000:1000 .env.prod` → `./prod.sh up -d --force-recreate api` → `./prod.sh up -d mcp-m365`.
7. **Verify** §6 — **fresh Entra login is mandatory** before any OBO/M365 check. Expect
   `Storing tool context … ~93 tools`, zero `AADSTS`, Sweden (not Switzerland) on tool-agent completions.

**Open Questions:**
- Push `pablo` separately, or let it ride along with the `memodo` FF? (Deploy-neutral either way —
  prod only ever pulls `memodo`. User deferred; default = rides along.)
- `docker-compose.yml:12` still pins the base image at `v0.8.6` (dead value — prod.yml and override.yml
  both set the api image). Cosmetic cleanup, deliberately not touched during the cutover.
- `docker-compose.override.yml` also declares `build:` + `librechat:upgrade`; its comment says to revert
  it alongside prod.yml when a compatible upstream image exists. Still no such image → leave as-is.

## Notes

- User's explicit instruction: **everything inside the 17:00 window** — no prod mutations were staged
  early. Commits were made locally only after the user asked ("Should we commit the changes now?").
- Deploy target is the **`memodo`** branch, not `pablo` (user requirement raised mid-session).
- Post-deploy, `docs/fork-upstream-divergences.md` entry 3 (M365 OBO fork-local patch to upstream MCP
  internals) still needs a manual re-check on every future upstream merge — CI does not run the merge
  path (SPEC-018 Item 15).
