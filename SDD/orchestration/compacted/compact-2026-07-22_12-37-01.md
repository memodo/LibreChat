# Session Compaction — MemodoAI "What's New (July 2026)" video recording (OBS, operator-driven) — 2026-07-22_12-37-01

## Session Context
- **Task:** Recording the companion video for the standalone **"What's New (July 2026)"** MemodoAI
  update deliverable. Operator-driven OBS recording: **the user owns the record button**; I drive the
  browser through the 7-chapter shot list in
  `docs/memodo-ai-tutorial/updates-2026-07/recording-script.md`.
- **Phase:** ad-hoc docs work — NOT an SDD phase.
- **Branch:** `pablo-oliva/docs-memodo-ai-tutorial`. All changes UNCOMMITTED (user commits/pushes manually).
- **Environment:** chat-test.memodo.de (v0.8.7-rc1 + M365 build; clone box `ssh memodo-eng-test`,
  178.105.91.145). Signed in via Microsoft SSO. Browser window 1440×900 (viewport ~1440×757).

## Recording protocol (chapter-by-chapter — user chose this)
1. I set up the exact starting frame, say **"ready"**.
2. User hits record in OBS.
3. User says **"go"** → I drive that one chapter, then **STOP and wait**.
4. User stops recording, cuts/reviews, says **"next"** → I set up the next chapter.
I never drive between takes. Optional mid-chapter add-ons offered via keywords (e.g. "run it", "show memories").

## Chapters — status
- **Ch 0 — Cold open** (static "Welcome to MemodoAI" frame): ✅ RECORDED.
- **Ch 1 — Microsoft 365**: ✅ RECORDED. Opened MCP Servers dropdown (M365 green/Active), ticked it,
  ran "Using Microsoft 365, what is my display name and job title?" → "Ran get-current-user in
  Microsoft365" + profile answer (Pablo Oliva / Team lead (technical) - Frontend Development). Clean.
- **Ch 2 — Skills**: ✅ RECORDED (Part A + Part B).
  - A: `$` picker → picked `meeting-notes` → fabricated transcript (Alex Chen/Sam Rivera/Jordan Lee) →
    "Used 2 tools — skill" → structured output (Attendees/Decisions/Action Items/Open Questions/Discussion Summary).
  - B: Skills panel (My Skills, +, Admin Settings) → +→"Write skill instructions" → filled a sample
    `warranty-claim` skill (When to use/Procedure/Output format) → **Cancel** (no real skill) → clicked
    `meeting-notes` → detail view (toggle/share/Edit/version/markdown body).
- **Ch 3 — Chaining & Memory**:
  - **Part A (Chaining)**: ✅ RECORDED. Agent Builder → selected `Chain Test Primary` → Advanced →
    Multi-agent orchestration → Chain (Chain Test Primary + Uppercase Bot). Then **"run it"**: Selected
    the agent, asked "What is the capital of France?" → chain ran: "Sir, the capital of France is Paris."
    then "AI: SIR, THE CAPITAL OF FRANCE IS PARIS." (Uppercase Bot transform). Clean.
  - **Part B (Memory)**: ❌ **SKIPPED / PARKED** — memory WRITE is broken on this build (silent bug).
    See "Memory bug" below. Do NOT record Part B until fixed or the chapter is reframed/cut.
- **Ch 4 — Projects**: ⏭️ **NEXT.** Not started.
- **Ch 5 — Quality-of-life**: pending.
- **Ch 7 — Wrap-up** (no captures): pending.

## RESUME HERE (next session)
Re-establish the browser, then continue at **Chapter 4 — Projects**:
1. Load Chrome MCP tools in ONE ToolSearch call (core set: tabs_context_mcp, navigate, computer,
   read_page, tabs_create_mcp; add file_upload + find for later chapters).
2. `tabs_context_mcp` (createIfEmpty:true if needed) → new tab → navigate https://chat-test.memodo.de/c/new.
3. Collapse the sidebar (top-left toggle, ~26,26) for clean menu shots; park cursor neutral. That's the clean frame.
4. Say "ready" and wait for the user's "go".
5. **Ch 4 shot** (recording-script.md Step 4.1 → `10-projects-sidebar.png`): the sidebar's **Projects**
   section → **New project** → name it `Q3 Planning` → **Create project** → its page opens ("New chat in
   [project]", "Chats 0 / No chats yet"). NOTE: showing Projects needs the sidebar EXPANDED (not
   collapsed) — Projects live in the conversation sidebar. Adjust: for Ch4 open the normal sidebar.
   Narration cue: Projects = folders grouping chats; Bookmarks = tags across chats.
6. Then **Ch 5 — Quality-of-life** (Steps 5.1–5.4): context-usage meter (hover the open circle in the
   composer → "Context — used/window (%)"); rich Office file preview (attach the sample XLSX → typed
   "Spreadsheet" preview card — sample at ~/Downloads/MemodoAI-sample-roadmap.xlsx AND copied to the
   session scratchpad; file_upload MCP tool works against the hidden input); message timestamps
   ("N minutes ago" by sender name); instant titles (narration cue only). Then **Ch 7 wrap-up** (no captures).

## Environment prerequisites (all VERIFIED this session, persist on chat-test)
- **M365 MCP**: connected to **Active** (green dot). In a NEW chat it shows connected-but-unticked → open
  MCP Servers dropdown, tick Microsoft365. OBO→Graph tool call verified working (no auth error).
- **`meeting-notes` skill** exists (My Skills, alongside `end-message`).
- **`Chain Test Primary` → `Uppercase Bot`** chain wired (Agent Builder → Advanced → Chain, 1/10).
- **Sample file**: `~/Downloads/MemodoAI-sample-roadmap.xlsx` (5181 bytes) + scratchpad copy.
- Browser gotcha: first keystroke after focusing composer sometimes doesn't register — verify text
  appeared before sending (esp. the `$` skill trigger).

## Memory bug (why Ch3 Part B is parked)
Memory **WRITE** does not persist on the v0.8.7 build (chat-test); **READ/injection works**. Full detail
in auto-memory **`project_memory_write_broken_v087`** (indexed in MEMORY.md). A complete, paste-ready
handoff prompt for a DEDICATED debug session was produced in-session (see the final assistant message of
the pre-compaction transcript) — the user is running that debug separately. Prime suspect surfaced: a race
where `api/server/cleanup.js` (~270) nulls `client.processMemory` before the fire-and-forget
`memoryPromise` (`api/server/controllers/agents/client.js` ~1541) completes → silent dropped write (fits
the "no logs at all" symptom). Fastest authoritative check: query MongoDB `memories` collection (container
`chat-mongodb`, userId 69fb1a872eeae17e31b9e578) before/after a save. **Impact:** prod adopts this build
at the v0.8.7+M365 cutover → memory will be broken on prod too; the tutorial's memory chapter
(recording-script Ch3.2 + narration §03 + updates-july-2026.md) claims it works → needs fix or reframing.

## Critical References
- `docs/memodo-ai-tutorial/updates-2026-07/recording-script.md` — 7-chapter shot list (source of truth for driving).
- `docs/memodo-ai-tutorial/updates-2026-07/recording-narration.md` — narration per section (timing/sync).
- `docs/memodo-ai-tutorial/updates-2026-07/UPDATE-PLAN-v0.8.7-m365.md` — plan/status.
- `docs/memodo-ai-tutorial/updates-2026-07/screenshots/` — 14 reference screenshots (01–12,14 + 03b,07b).
- auto-memory: `project_july2026_updates_tutorial`, `project_memory_write_broken_v087`, `project_test_env_chat_test`.

## Remaining after recording (unchanged from prior compaction)
Narration render → assemble cut → transcript/chapters → transcode → bilingual HTML export → `/guide`
selector (Guide.tsx: "Getting Started" | "What's New (July 2026)") → deploy (chat-test now; prod at the
pending v0.8.7+M365 cutover). Plus evergreen getting-started/ corrections (§7) and the stale
`docs/m365-mcp-features.md` "verification pending" header.

## Notes
- All work UNCOMMITTED on `pablo-oliva/docs-memodo-ai-tutorial`; user commits/pushes manually.
- OBS footage of Ch 0/1/2/3A lives on the user's machine, not in the session — safe across /clear.
