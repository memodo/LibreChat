# Session Compaction - MemodoAI tutorial: transcript + chapters (English) - 2026-05-27 13:16:05

## Session Context

- Context trigger: high utilization during follow-up work
- Working on: MemodoAI end-user tutorial — produced written guide, screenshots, narration audio, an OBS-recorded video, then transcribed the video audio and derived chapters
- Current phase: ad-hoc (not an SDD phase). Branch: `pablo`

## Work Summary

### Completed This Session
- **Committed** the tutorial package (2 commits on `pablo`):
  - `3f6d59aa5` — tutorial package (guide, 32 screenshots, TTS tooling, gitignore retarget to track source / ignore media)
  - `ab34bb6be` — split README: `memodo-ai-tutorial.md` (the guide) vs. `README.md` (directory overview + production workflow)
- **Recorded the video** (operator-controlled OBS while Claude drove the browser via the Claude-in-Chrome extension). Earlier `screencapture -v` attempt lost a take — see `feedback_screencapture_termination` memory.
- **Pablo edited** audio+video in Shotcut, added Claude-Design intro/outro. Exported:
  - `docs/memodo-ai-tutorial/MemodoAI intro.mpg` (317 MB, 16:56 video) — gitignored
  - `docs/memodo-ai-tutorial/MemodoAI intro.mp3` (23 MB, 17:04 audio) — gitignored
- **Transcribed** the audio locally with whisper.cpp (`whisper-cli` + `ggml-large-v3`), via new `docs/memodo-ai-tutorial/tooling/transcribe.sh`. 367 cues.
- **Corrected** transcription artifacts in `.srt/.vtt/.txt` (left `.json` raw): brand "Momodo/Momoto/Memedo AI" → MemodoAI; "Shea DCN" → shadcn/ui; "used end tools" → "Used N tools"; "what write looks like" → "what right looks like"; redakt service name.
- **Derived 15 chapters** → `transcript/chapters.en.txt` + `transcript/chapters.json`.

### In Progress
- **Pinning the English version** before any German work. Awaiting Pablo review of: transcript accuracy, the 15 chapter titles/boundaries, and the guide↔video discrepancy (below).

### Remaining (DEFERRED per Pablo — keep open)
- **Task #23** — German translation of transcript + chapters (informal *du*): `transcript.de.{srt,vtt,txt}`, `chapters.de.txt`
- **Task #24** — German written guide `memodo-ai-tutorial.de.md` (informal *du*)
- Both deferred until the English versions are finalized.

## Key Discoveries

- **Guide ↔ video divergence (decide before pinning English):** the video/audio **skips the "Common workflows" section** (narration goes PII warnings → wrap-up tips). The written guide `memodo-ai-tutorial.md` still has `## 14. Common workflows`. Options: leave guide as a superset / trim it to match video / re-record that section.
- Transcript timestamps are **MP3-relative**; the `.mpg` is ~8 s shorter. Timings will shift again when Pablo adds title cards (his step 3) and re-exports — re-run `transcribe.sh` then, and re-translate German.
- whisper.cpp is local (no API key): `whisper-cli` at `/opt/homebrew/bin`, model `~/.local/share/whisper-cpp/models/ggml-large-v3.bin` (overridable via `WHISPER_MODEL`).
- German register decision: **informal "du"** for all German output; keep English UI feature names (Memories, Prompts, File Search, etc.) since the UI is English.

## Critical Review Status
- Review performed: no (not an SDD phase; no CRITICAL-*.md applicable)
- Unresolved findings: n/a

## Critical References

- `docs/memodo-ai-tutorial/README.md` — directory overview + production workflow
- `docs/memodo-ai-tutorial/memodo-ai-tutorial.md` — the English guide (has the Common-workflows section in question)
- `docs/memodo-ai-tutorial/transcript/transcript.en.{srt,vtt,txt,json}` — transcript (srt/vtt/txt corrected; json raw)
- `docs/memodo-ai-tutorial/transcript/chapters.en.txt` + `chapters.json` — 15 chapters
- `docs/memodo-ai-tutorial/tooling/transcribe.sh` — whisper.cpp transcription wrapper (re-run after step-3 re-export)
- `docs/memodo-ai-tutorial/tooling/tts/narrate.mjs` — ElevenLabs narration renderer (cwd-independent)

## Next Session

**Resume From:**
- English review/pinning. The transcript + 15 chapters are produced and corrected; waiting on Pablo's sign-off.

**Immediate Priorities:**
1. Get Pablo's decision on the guide↔video "Common workflows" discrepancy.
2. Apply any transcript/chapter edits Pablo requests; lock the English text.
3. Commit the English transcript + chapters + `transcribe.sh` (currently untracked: `transcript/`, `tooling/transcribe.sh`).
4. Only then start the deferred German work (tasks #23, #24): translate transcript/chapters/guide to informal *du*.

**Open Questions:**
- Keep, trim, or re-record the "Common workflows" section so guide and video agree?
- Any other transcript term corrections beyond those already applied?
- Generate German subtitles now (current timings, first pass) or wait until after step-3 re-timing?

## Notes

- Uncommitted now: `docs/memodo-ai-tutorial/transcript/` and `docs/memodo-ai-tutorial/tooling/transcribe.sh` (both trackable — not media). The `.mp3/.mpg` and `audio//video/` media stay gitignored.
- Pablo's remaining manual steps in his own plan: step 3 (generate title/transition cards from chapters; re-export audio+video with shifted timings; subtitles will be sidecars, NOT burned in) and step 5 (assemble final written guides in Claude Design from EN+DE guide + transcripts).
- Commit style on this repo: `scope: description`, no co-author/attribution lines, never push without asking; feature work lands on `pablo`.
