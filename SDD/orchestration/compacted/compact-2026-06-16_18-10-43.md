# Session Compaction — LibreChat v0.8.5→v0.8.6 upstream upgrade — 2026-06-16 18:10

## Session Context

- Context trigger: high utilization during a long upgrade session.
- Working on: upstream upgrade LibreChat v0.8.5 → **v0.8.6** (ad-hoc, NOT an SDD phase).
- Current phase: follow-up / ad-hoc (local validation + testing).
- Worktree: this file lives in `LibreChat-worktree` on branch `feature-upgrade-15-06-26`
  (uncommitted by request). **Primary detail = the `project_v086_upgrade` memory** — read it
  first; this is just the resume pointer.

## Work Summary

### Completed This Session
- First merge tracked `origin/main` HEAD → crash-looped (HEAD is past the v0.8.6 release;
  pulled in unreleased tsdown `index.cjs` + `get-stream` that no published image ships).
- **Re-targeted to the v0.8.6 release TAG** (`566e20b6`); matches the v0.8.6 image
  (old bundler, `@librechat/api` → `dist/index.js`, no tsdown/unrun/get-stream). 6 conflicts,
  all resolved (subset of the 14 from the HEAD merge).
- Bumped base image `v0.8.5`→`v0.8.6` in `docker-compose.yml` + `docker-compose.prod.yml`.
- Post-merge test fix: `api/server/routes/__tests__/pii-middleware-chain.spec.js` — added
  `jest.mock('~/server/routes/agents/middleware', …)` passthroughs.
- **Validated:** full build green (api dist=`index.js`, matches image), static check
  (`scripts/pii-merge-verify.sh`), PII unit+integration **86**, data-schemas **151**, stack
  **boots clean** on the v0.8.6 image, redakt detects PII (contract matches the middleware).
- Reverted `pablo` (`16a417f8f`) and `memodo` (`be0f8577a`) to pre-upgrade on local + remote.
  Entire upgrade isolated on **`feature-upgrade-15-06-26` = `717b04527`** (local only).

### In Progress / Remaining
- **Run the v0.8.6 stack locally and test PII end-to-end with redakt** (redakt is UP) — the
  immediate goal. Stack boots clean; needs the feature branch + v0.8.6 dist (synced,
  gitignored) + v0.8.6 image (pulled) + container recreate. NOTE: the local Docker stack is
  wired to the SIBLING worktree `/Users/pablooliva/Dev/AI dev/LibreChat`, so testing via
  Docker means either checking this branch out there or re-pointing the mounts here.
- Push `feature-upgrade-15-06-26` — BLOCKED: PAT lacks `workflow` scope (merge touches ~19
  `.github/workflows/*`). Fix: add scope OR strip workflow-file changes.
- Prod deploy: DEFERRED (needs image bump + workflow-scoped push or alternate path).

## Key Discoveries
- **Pin to the release TAG, not `origin/main` HEAD.** HEAD is a moving dev branch past the
  release; its build/deps don't pair with any published release image.
- Deploy model overlays built `dist/` onto the upstream release image but bind-mounts only
  `dist/` (not `package.json`) — the build's entry filename must match the image
  (`@librechat/api` = `index.js` at the v0.8.6 tag; data-provider=`index.js`,
  data-schemas=`index.cjs`).
- `registry.librechat.ai/danny-avila/librechat:latest` == `:v0.8.6` (same digest). A
  `:v0.8.7-rc1` image exists (pre-release).
- e2e blocked: `E2E_USER_EMAIL=p.oliva@memodo.de` is SSO/Entra, no local password
  (`POST /api/auth/login` → 404) → form login fails. Need a local-password test user
  (`ALLOW_REGISTRATION=false`, create via container).

## Critical Review Status
- Review performed: no formal SDD critical review (ad-hoc upgrade).
- Unresolved findings: none code-wise; open items are operational (workflow-scope push,
  prod deploy, full browser e2e).

## Critical References
- Memory: `project_v086_upgrade` (primary, full detail).
- Branch: `feature-upgrade-15-06-26` @ `717b04527` (local only, uncommitted compaction/progress changes).
- `docker-compose.yml:12` + `docker-compose.prod.yml:18` — image pinned `v0.8.6`.
- 6 conflict files (resolved): `api/server/index.js`, `api/server/routes/agents/openai.js` +
  `responses.js`, `client/src/hooks/Nav/useSideNavLinks.ts`,
  `packages/data-provider/src/keys.ts`, `packages/data-schemas/src/admin/capabilities.ts`.
- `packages/api/tsdown.config.mjs` is NOT present at the v0.8.6 tag (classic bundler).

## Next Session

**Resume From:**
- Branch `feature-upgrade-15-06-26` (the entire upgrade). Decide whether to test via Docker in
  the sibling worktree (stack is wired there) or re-point the stack at this worktree.

**Immediate Priorities:**
1. Stand up the v0.8.6 stack (v0.8.6 image already pulled, v0.8.6 dist already built/synced):
   `docker compose up -d --no-deps --force-recreate api`; confirm boot
   (`docker inspect LibreChat` status=running, restarts=0; `curl localhost:3080/health` → OK).
2. Manual PII smoke with redakt up: `PII_DETECTION_MODE=warn` → send "My name is John Smith"
   → warning toast + message goes through + guardrail event in admin dashboard; then `detect`
   mode → message blocked.
3. (Optional) full browser e2e — needs a local-password test user first.

**Open Questions:**
- How to land `feature-upgrade-15-06-26` on the remote (add `workflow` token scope vs strip
  workflow-file changes)?
- Test via the sibling worktree, or re-point the Docker stack at this worktree?

## Notes
- `pablo`/`memodo` must STAY pre-upgrade; the upgrade lives only on `feature-upgrade-15-06-26`.
- This compaction file + the fresh `progress.md` are UNCOMMITTED by request.
- The previous `progress.md` (SPEC-016 Conversation Log Sidecar, 702 lines) was archived to
  `SDD/orchestration/progress-archived-2026-06-16_18-10-43.md` — unrelated to this upgrade.
